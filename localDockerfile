FROM ruby:3.1
RUN apt update -qq && apt install -y postgresql-client ruby-dev vim \
    && apt clean \
    && rm -rf /var/lib/apt/lists/*

ENV HOMEDIR "/app"
RUN mkdir ${HOMEDIR}
WORKDIR ${HOMEDIR}

EXPOSE 3000
